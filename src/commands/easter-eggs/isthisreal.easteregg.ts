export default function isThisRealEasterEgg(bot) {
    bot.command(['isthisreal', 'isthisreal?'], ({ reply }) => reply('Of course it\'s happening inside your head, Harry. But why on Earth would that mean it\'s not real?'));
}